#include <stdio.h>

int main()
{
        int i, j, target, invert = 0, num = 10; // 999 * 999

        for ( ; num > 1; num--)
        {
                printf("%d\n", num);
                i = num;
        }

        printf("%d",)
}